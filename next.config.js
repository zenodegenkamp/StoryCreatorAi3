/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  // ESLint is disabled for experimental use.
  // eslint: false,
};

module.exports = nextConfig;  